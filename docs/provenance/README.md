# Provenance

`source_inventory.json` identifies the isolated V3C-V3AG source files from which the public modules were extracted or adapted. Paths are relative to the isolated source root and contain no workstation or cluster identifiers.
