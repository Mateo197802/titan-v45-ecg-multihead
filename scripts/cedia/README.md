# CEDIA Scripts

CEDIA wrappers generate reproducibility and cleanup manifests. Cleanup requires a separate guarded command and stops when jobs, hashes, assets, or tests are not in the required state.
