# Public Profiles

Each JSON file freezes task, evaluation classes, artifact class order, thresholds, canonical status, and exact backbone/specialist SHA-256. `rhythm_primary6_diagnostic` retains an eight-output rhythm artifact but evaluates a declared six-class view.
