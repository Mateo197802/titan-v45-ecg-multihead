# Head Contracts

This directory contains the released global-output contract for the heads carried by the V3F and V3AG backbones. Head weights are packaged inside the backbone Release assets; the JSON contract fixes output names, class order, and dimensionality for independent validation.
