# Python Package

The package contains reusable implementation code. Command-line argument parsing remains in `scripts/`; scientific contracts and primary metric definitions remain inside the package so tests can enforce them directly.
