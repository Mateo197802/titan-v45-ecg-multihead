# Data Module

Implements signal preprocessing and manifest validation. Public manifests use release-relative record paths or stable record identifiers and must never embed workstation or CEDIA paths.
